import styled from "styled-components";

export const AppSectionTitle1 = styled.h1`
  font-size: 3.6rem;
  /* background-color: ${(p) => p.theme.darkest}; */
  display: inline-block;
  padding: 0.5rem 1rem;
  text-transform: uppercase;
  margin: 0;
`;
