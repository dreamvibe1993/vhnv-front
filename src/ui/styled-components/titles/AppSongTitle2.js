import styled from "styled-components";

export const AppSongTitle2 = styled.h2`
  font-size: 2.4rem;
  background-color: ${(p) => p.theme.darkest};
  display: inline-block;
  padding: 0.5rem 1rem;
`;
